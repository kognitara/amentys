[package]
name = "ammit"
description = "Kill-switch layer: enforces fatal cutoffs and emergency shutdowns"
license-file.workspace = true
edition.workspace = true
publish.workspace = true
version.workspace = true
keywords = ["ammit", "kill-switch", "shutdown", "security"]

[lib]
bench = false
doctest = true
name = "ammit"
path = "src/lib.rs"
test = true

[dependencies]
