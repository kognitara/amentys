[package]
name = "douat"
version = "0.1.0"
edition = "2024"
description = "Spectral network layer: hidden paths and covert routing channels"
license = "LicenseRef-ASAL"
license-file = "../LICENSE"
keywords = ["douat", "network", "routing", "covert"]

[lib]
bench = false
doctest = true
name = "douat"
path = "src/lib.rs"
test = true

[dependencies]
