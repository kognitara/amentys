[package]
name = "hu"
version = "0.1.0"
edition = "2024"
description = "Library package manager: assembles plan recipes from reusable modules"
license = "LicenseRef-ASAL"
license-file = "../LICENSE"
keywords = ["hu", "package", "manager", "plan"]
[dependencies]
x86_64 = { version = "0.15.5", default-features = false, features = [
    "instructions",
] }
