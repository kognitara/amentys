[package]
name = "kpack"
description = "Delta-compression engine for immutable patching and image assembly"
keywords = ["kpack", "compression", "patch", "immutable"]
license-file.workspace = true
edition.workspace = true
publish.workspace = true
version.workspace = true

[lib]
bench = false
doctest = true
path = "src/lib.rs"
test = true

[dependencies]
