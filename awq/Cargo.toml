[package]
edition = "2024"
name = "awq"
version = "0.1.0"
description = "Branch ledger: manages immutable and ephemeral Merkle tree states"
keywords = ["awq", "merkle", "branch", "ledger"]
[lib]
bench = false
doctest = true
name = "awq"
path = "src/lib.rs"
test = true

[dependencies]
heapless = {version = "0.9.3", features = []}
noun = {version = "0.1.0", path = "../noun"}
